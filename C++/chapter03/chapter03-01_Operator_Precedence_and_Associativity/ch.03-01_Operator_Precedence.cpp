#include <iostream>
#include <cmath>
int main()
{
    using namespace std;

    // int x = 4 + 2 * 3; // add(4, mult(2,3))

    int y = 3 * 4 / 2; 

    int x = std::pow(2,3); // return 값이 double
    cout <<  x << endl; // 8

    // 연습문제

    int r = 1 + 2 + 3 * 4;
    cout << r << endl;

    // int a = b = c;

    int w = 10;
    // int t /= --w + 5;
    // cout << t << endl;

    // a || b && c || d

    return 0;
}